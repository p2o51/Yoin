<?xml version="1.0" encoding="utf-8"?>
<!-- The shell beneath a dock-handoff open: hold still at full opacity while
     the detail window crossfades in above. Same duration as the enter half. -->
<alpha xmlns:android="http://schemas.android.com/apk/res/android"
    android:fromAlpha="1.0"
    android:toAlpha="1.0"
    android:duration="240" />
